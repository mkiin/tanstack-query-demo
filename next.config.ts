import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  typedRoutes: true,
  turbopack: {
    root: "/home/yaruha/project-dev/web-app/frontend-playground/tanstack-query-demo",
  },
};

export default nextConfig;
